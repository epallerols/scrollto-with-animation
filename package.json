{
  "name": "scrollto-with-animation",
  "version": "1.6.1",
  "description": "Animated scrolling without any dependency on libraries",
  "main": "dist/bundle.js",
  "scripts": {
    "build": "browserify src/scrollto-with-animation.js | uglifyjs -cm > dist/bundle.js",
    "prepublish": "rm -rf dist/bundle.js && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/davesnx/scrollto-with-animation"
  },
  "bugs": {
    "url": "https://github.com/davesnx/scrollto-with-animation/issues"
  },
  "keywords": [
    "animation",
    "requestAnimationFrame",
    "cross-browser",
    "setTimeout",
    "easeInQuad",
    "browserify"
  ],
  "author": "David Sancho <dsnxmoreno@gmail.com> (https://github.com/davesnx)",
  "license": "MIT",
  "dependencies": {
    "animation-frame": "^0.2.5"
  },
  "devDependencies": {
    "browserify": "^12.0.1",
    "uglify-js": "^2.6.1"
  },
  "files": [
    "dist/bundle.js"
  ]
}
